node_modules/
# Build output: rebuilt by `make build`. Committed only at release tags (see
# docs-internal/migration-to-ts-high-level.md — Distribution).
dist/
# Transpiled unit tests (npm test output).
dist-test/
# Parity harness bundle (make parity output).
dist-parity/
.tmp/
