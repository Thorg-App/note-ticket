node_modules/
# Build output: never committed. Distribution is build-from-source, and `./ticket`
# rebuilds this on demand whenever it is missing or older than src/.
dist/
# Transpiled unit tests (npm test output).
dist-test/
# Library build (npm package entry): JS + .d.ts emitted by `npm run build:lib`.
dist-lib/
.tmp/
